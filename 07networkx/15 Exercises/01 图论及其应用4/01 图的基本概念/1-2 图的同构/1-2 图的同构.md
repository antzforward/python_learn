# 1-2 图的同构   
***   

## 习题 1.2
### 1.2.1 
(a）证明：若$D\cong H$,则$v(D)=v(H),且\varepsilon(D) = \varepsilon(H)$
证明：假设 $D \cong H$，即存在一个同构映射 $f: V(D) \to V(H)$，其中 $f$ 是双射，且对于任意 $u, v \in V(D)$，有 $(u, v) \in E(D)$ 当且仅当 $(f(u), f(v)) \in E(H)$。

首先，由于 $f$ 是双射，它建立了顶点集之间的一一对应关系，因此顶点数相等，即：
$$
v(D) = |V(D)| = |V(H)| = v(H).
$$

其次，由于同构映射 $f$ 将边一一对应地映射，即每条边 $(u, v) \in E(D)$ 唯一对应一条边 $(f(u), f(v)) \in E(H)$，且由于 $f$ 是双射，这种对应是双射，因此边数相等，即：
$$
\varepsilon(D) = |E(D)| = |E(H)| = \varepsilon(H).
$$

因此，若 $D \cong H$，则 $v(D) = v(H)$ 且 $\varepsilon(D) = \varepsilon(H)$。

$$\boxed{v(D)=v(H)} \quad \boxed{\varepsilon(D)=\varepsilon(H)}$$
(b)  举例说明(a)的逆命题不真
### 证明：若 $D \cong H$，则 $v(D) = v(H)$ 且 $\varepsilon(D) = \varepsilon(H)$

假设 $D \cong H$，即存在一个同构映射 $f: V(D) \to V(H)$，其中 $f$ 是双射，且对于任意 $u, v \in V(D)$，有：
$$
(u, v) \in E(D) \iff (f(u), f(v)) \in E(H).
$$

- **顶点数相等**：由于 $f$ 是双射，它建立了顶点集之间的一一对应关系，因此：
  $$
  v(D) = |V(D)| = |V(H)| = v(H).
$$

- **边数相等**：由于同构映射 $f$ 将边一一对应地映射，即每条边 $(u, v) \in E(D)$ 唯一对应一条边 $(f(u), f(v)) \in E(H)$，且这种对应是双射，因此：
  $$
  \varepsilon(D) = |E(D)| = |E(H)| = \varepsilon(H).
$$

综上，若 $D \cong H$，则 $v(D) = v(H)$ 且 $\varepsilon(D) = \varepsilon(H)$。

---

### (b) 举例说明 (a) 的逆命题不真

逆命题：若 $v(D) = v(H)$ 且 $\varepsilon(D) = \varepsilon(H)$，则 $D \cong H$。

该逆命题不成立。以下是一个反例：

- 令 $D$ 为路径图 $P_4$（4个顶点，3条边）：
  $$
  V(D) = \{1, 2, 3, 4\}, \quad E(D) = \{(1,2), (2,3), (3,4)\}.
$$

- 令 $H$ 为星图 $K_{1,3}$（4个顶点，3条边）：
  $$
  V(H) = \{A, B, C, D\}, \quad E(H) = \{(A,B), (A,C), (A,D)\}.
$$

则：
- $v(D) = 4 = v(H)$，
- $\varepsilon(D) = 3 = \varepsilon(H)$，

但 $D$ 与 $H$ 不同构，因为 $P_4$ 的最大度为 2，而 $K_{1,3}$ 的最大度为 3，无法通过顶点重标号使边集一致。

因此，逆命题不成立。

$$
\boxed{P_4} \quad \text{和} \quad \boxed{K_{1,3}}
$$
(c)  分别构造出两个同构的有向图和无向图
**同构的无向图示例**
- 图 $G_1$：顶点集 $\{A, B, C\}$，边集 $\{(A,B), (B,C), (C,A)\}$（三角形）。
- 图 $G_2$：顶点集 $\{1, 2, 3\}$，边集 $\{(1,2), (2,3), (3,1)\}$。
- 同构映射：$f(A)=1, f(B)=2, f(C)=3$。这两个图同构，因为结构相同。

**同构的有向图示例**
- 图 $D_1$：顶点集 $\{X, Y, Z\}$，边集 $\{(X,Y), (Y,Z), (Z,X)\}$（有向环）。
- 图 $D_2$：顶点集 $\{P, Q, R\}$，边集 $\{(P,Q), (Q,R), (R,P)\}$。
- 同构映射：$g(X)=P, g(Y)=Q, g(Z)=R$。这两个图同构，因为边方向一致且结构相同。

### 1.2.2 证明：两个简单图D和H同构$\Leftrightarrow$存在双射$\theta$:$V(D)\rightarrow V(H),使得(x,y)\in E(D)\Leftrightarrow (\theta(x),\theta(y))\in E(H)$
**证明**：
图同构的定义直接基于顶点之间的双射和边关系的保持。以下从必要性和充分性两个方面证明。

- **必要性（$\Rightarrow$）**：  
  假设 $D$ 和 $H$ 同构。根据图同构的定义，存在一个双射 $\theta: V(D) \rightarrow V(H)$，使得对于任意两个顶点 $x, y \in V(D)$，边 $(x,y) \in E(D)$ 当且仅当边 $(\theta(x), \theta(y)) \in E(H)$。这正好满足给定条件。

- **充分性（$\Leftarrow$）**：  
  假设存在双射 $\theta: V(D) \rightarrow V(H)$，使得对于任意 $(x,y) \in E(D)$，有 $(\theta(x), \theta(y)) \in E(H)$，且反之亦然。这意味着图 $D$ 和 $H$ 在顶点重标号 $\theta$ 下具有相同的边结构，因此根据定义，$D$ 和 $H$ 同构。

综上，所述条件等价于图同构。

$$\boxed{\theta}$$
### 1.2.3 证明：图1.2和图1.3中所示的五个图以及下列三个图都是Peterson图同构。
我先说明Peterson图。书上并没有明确定义，根据资料结合来讲就是：10个点，3-正则，围长5（就是经过5条边都回到原点，最短）![[1-2-3-2.png]]
![[1-2-3-1.png]]

我都是使用Petersen生成图的方式制作的，并且增加了后面题目的顶点的标注（label），显然满足要求

### 1.2.4 证明：若G是简单2部图，则$$\varepsilon (x)\le 
\begin{cases}
\frac{1}{4}v^2  & \text{若 } v \text{是偶数 } \\
\frac{1}{4}(v^2-1) & \text{若 } v \text{是奇数}
\end{cases}$$
证明：如果能把一个图 $G$ 的顶点集 $V$ 划分成 $k$ 个​**​非空​**​子集 $V_1,V_2,...,V_k$​（称为​**​部集​**​），使得 $G$ 的每条边的两个端点都分别属于​**​不同的部集​**​，则称 $G$ 为 ​**​k部图​**​。这种划分 ($V_1,V_2,...,V_k$​) 称为一个 ​**​k部划分​**​。
 ​**​2部图（Bipartite Graph）​**​: $k=2$ 时的 $k$ 部图。当2部图为完全2部图的时候
 $V_1+V_2=v$ 且$\varepsilon = V_1\times V_2=\frac{1}{4}v^2-(v-V_1)^2$
 当$v$为偶数时，$\varepsilon = \frac{1}{4}v^2$成立
 当$v$为奇数时，令$V_1= \frac{1}{2}(v-1),V_2= \frac{1}{2}(v+1)$时$\varepsilon = \frac{1}{4}v^2$成立
 所以简单2部图的边数小于等于完全2部图，成立
### 1.2.5 写出k部图和完全$K_n(k)$的定义，并证明：
#### (a)设G是简单k部图，每部分分别为$n_1,n_2,\dots,n_k$ 则$$\varepsilon(G)\le \frac{1}{2}\sum_{i=1}^{k} n_i\times(v-n_i)$$
证明：如果能把一个图 $G$ 的顶点集 $V$ 划分成 $k$ 个​**​非空​**​子集 $V_1,V_2,...,V_k$​（称为​**​部集​**​），使得 $G$ 的每条边的两个端点都分别属于​**​不同的部集​**​，则称 $G$ 为 ​**​k部图​**​。这种划分 ($V_1,V_2,...,V_k$​) 称为一个 ​**​k部划分​**​。
完全k部图（Complete k-partite Graph）​**​: 在一个 $k$ 部图中，如果​**​任意两个位于不同部集的顶点​**​之间都恰好有一条边相连，则称为完全 $k$ 部图。记作 $K_{n_1,n_2,...,n_k}$​​，其中 $n_i=|V_i|$。
所以在完全k部图中，边数最大，当选i部集是，此部集的边数为$\frac{1}{2}n_i\times(v-n_i)$
所以完全k部图的边为$\frac{1}{2}\sum_{i=1}^{k} n_i\times(v-n_i)$,普通图与完全图的关系是顶点相同，普通图的边集是完全图的子集的情况，所以边数$\le$ 完全图的边数 成立。
#### (b) $$\varepsilon(K_n(k))=\frac{1}{2}k(k-1)n^2$$完全k部图（Complete k-partite Graph）​**​: 在一个 $k$ 部图中，如果​**​任意两个位于不同部集的顶点​**​之间都恰好有一条边相连，则称为完全 $k$ 部图。记作 $K_{n_1,n_2,...,n_k}$​​，其中 $n_i=|V_i|$。 其中$n_i=\frac{n}{k}$ 因为是完全k部图，则$\varepsilon(K_n(k)) = \frac{1}{2}\sum_{i=1}^{k} \frac{v}{k}\times(v-\frac{v}{k})=\frac{1}{2}k\times\frac{v}{k}\times(v-\frac{v}{k})=\frac{1}{2}\frac{v^2\times(k-1)}{k}$
$\because v=n\times k,\therefore \varepsilon(K_n(k)) = \frac{1}{2}k(k-1)n^2$ 成立。

### 1.2.6 如果$D\cong D^c$（或$G\cong G^c$）,则称简单图D(或G)为自补的（self complementary）证明
#### （a）若D是自补图，则$\varepsilon=\frac{1}{2}v(v-1)$
完全图D的边数为$\varepsilon(K_v)=v(v-1)$,由于自补图，则$\varepsilon(D)=\varepsilon(D^c)\text{且}\varepsilon(D)+\varepsilon(D^c)=\varepsilon(K_v)$
得证。
#### (b)若G是自补图，则$\varepsilon=\frac{1}{4}v(v-1)\text{且}v\equiv  0,1(mod 4)$
与上面相同，注意$v(v-1)$能被4整除，所以$v\equiv  0,1(mod 4)$ 必须。

### 1.2.7 设$H(n,i)$是例1.2.4中定义的图，证明：
#### （a）对$H(n,i)$中任意相邻的两个顶点X和Y，|X|和|Y|有相同的奇偶性$\Leftrightarrow$i是偶数
$H(n,i)$的定义：
#### （b）若n是偶数且i是奇数，则$H(n,i)\cong H(n,n-i)$

### 上面几个概念不在第二版里面，在第四版里面就先不做了

